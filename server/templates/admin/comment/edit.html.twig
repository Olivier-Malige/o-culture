{% extends 'base.html.twig' %}

{% block title %}>Modifier le commentaire{% endblock %}

{% block body %}
    <h1>Modifier le commentaire</h1>

    {{ include('admin/comment/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
   
    <div class="d-flex mt-4">
        <a href="{{ path('comment_index', {page: 1}) }}" class="btn btn-dark mr-1">Retour à la liste des événements</a>
        {{ include('admin/comment/_delete_form.html.twig') }}
    </div>
{% endblock %}
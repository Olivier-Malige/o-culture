{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
{{ form_row(form.content) }}
{{ form_row(form.receiver) }}

    <button class="btn">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}
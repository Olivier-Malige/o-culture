<div class="mb-4">
    <a href="{{ path('place_index', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-user"></i> Tous les lieux</a>
    <a href="{{ path('place_type_index', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-tags"></i>  Types de lieux</a>
    <a href="{{ path('place_new') }}" class="btn btn-success mr-1">Ajouter un nouveau lieu</a>
</div>
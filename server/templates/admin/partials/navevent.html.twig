<div class="mb-4">
    <a href="{{ path('event_index', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-user"></i> Tous les événements</a>
    <a href="{{ path('event_type_index', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-tags"></i>  Types d'événements</a>
    <a href="{{ path('tag_index', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-hashtag"></i> Tags d'événements</a>
    <a href="{{ path('event_new') }}" class="btn btn-success mr-1">Ajouter un nouvel événement</a>
</div>
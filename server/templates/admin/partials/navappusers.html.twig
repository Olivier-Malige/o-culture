<div class="mb-4">
    <a href="{{ path('app_user_index', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-user"></i> Tous les utilisateurs</a>
    <a href="{{ path('app_user_index_appusers', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-user-circle"></i> Spectateurs</a>
    <a href="{{ path('app_user_index_organizers', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="far fa-address-card"></i> Organisateurs</a>
    <a href="{{ path('app_user_index_artists', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-theater-masks"></i> Artistes</a>
    <a href="{{ path('artist_type_index', {'page': 1}) }}" class="btn btn-secondary mr-1"><i class="fas fa-certificate"></i> Types d'artistes</a>
    <a href="{{ path('app_user_new') }}" class="btn btn-success mr-1">Ajouter un nouvel utilisateur</a>
</div>
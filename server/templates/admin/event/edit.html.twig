{% extends 'base.html.twig' %}

{% block title %}Modifier l'événement - {{ event.name }}{% endblock %}

{% block body %}
    <h1>Modifier l'événement - {{ event.name }}</h1>

        {{ include('admin/event/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

    <div class="d-flex mt-4">
        <a href="{{ path('event_index', {page: 1}) }}" class="btn btn-dark mr-1">Retour à la liste des événements</a>
        {{ include('admin/event/_delete_form.html.twig') }}
    </div>
{% endblock %}
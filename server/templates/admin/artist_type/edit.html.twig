{% extends 'base.html.twig' %}

{% block title %}Modifier {{ artist_type.name }}{% endblock %}

{% block body %}
    <h1>Modifier {{ artist_type.name }}</h1>

    {{ include('admin/artist_type/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

    <div class="d-flex mt-4">
        <a href="{{ path('artist_type_index', {page: 1}) }}" class="btn btn-dark mr-1">Retour à la liste des événements</a>
        {{ include('admin/artist_type/_delete_form.html.twig') }}
    </div>

{% endblock %}
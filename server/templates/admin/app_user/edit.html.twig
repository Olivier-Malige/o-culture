{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur{% endblock %}

{% block body %}
    <h1>Modifier l'utilisateur</h1>

    {{ include('admin/app_user/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

    <div class="d-flex mt-4">
        <a href="{{ path('app_user_index', {page: 1}) }}" class="btn btn-dark mr-1">Retour à la liste des événements</a>
        {{ include('admin/app_user/_delete_form.html.twig') }}
    </div>
{% endblock %}
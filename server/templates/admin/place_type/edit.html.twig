{% extends 'base.html.twig' %}

{% block title %}Modifier le type de lieu - {{ place_type.name }}{% endblock %}

{% block body %}
    <h1>Modifier le type de lieu - {{ place_type.name }}</h1>

    {{ include('admin/place_type/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <div class="d-flex mt-4">
        <a href="{{ path('place_type_index', {page: 1}) }}" class="btn btn-dark mr-1">Retour à la liste des types de lieux</a>
        {{ include('admin/place_type/_delete_form.html.twig') }}
    </div>

{% endblock %}